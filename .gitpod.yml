image:
  file: .gitpod.Dockerfile

tasks:
  - name: Maven Package
    init: ./mvnw package
  #- name: Docker Pull
  #  init: docker-compose -f ./devtools/docker-compose.yml pull

vscode:
  extensions:
    - redhat.java
